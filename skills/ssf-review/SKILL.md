---
name: ssf-review
description: 审查代码、工作区 diff 或 PR，核实正确性和需求符合度；不自动组织多 agent 审查。
---

# ssf-review

先确定审查范围：使用用户给定 diff、PR 或基线；未指定时优先当前未提交改动，其次当前分支相对明确目标分支的差异。不能可靠确定目标分支时询问，不猜测审查范围。

按 [审查参考](references/code-review.md) 检查两方面：代码正确性与项目规范、是否满足会话或已有计划的需求。缺少独立规格文件不阻塞审查。

给出可定位的文件、行号、触发条件、影响和建议；把事实缺陷与风格偏好分开。没有发现问题时直接说明检查范围和剩余验证缺口，不凑问题数量。

默认本会话检查，不强制 issue tracker 或并行子 agent。用户要求独立核验时，给 reviewer 足够的需求、diff 和真实验证证据；不得把自己的检查称为独立审查。收到反馈后先复现或核实，再在授权范围内修复。

通常在会话中输出；跨会话需要记录时补到已有计划，不要求多份 request/evidence/signoff 文件。
