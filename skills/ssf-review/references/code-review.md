# 按需代码审查

采用 mattpocock/skills 从规范和需求两方面检查 diff 的方法，去掉强制 issue tracker、双子 agent 和固定格式依赖。

- 正确性与项目规范：检查条件分支、错误路径、异步生命周期、数据一致性、权限及必要测试。设计坏味道只是线索，必须结合实际代码和宿主约定，不为了抽象而要求重构。
- 需求符合度：以用户会话、已有计划或指定 issue 为依据，检查缺失行为、实现错误和超出范围的修改。不要求独立规格文件或 Spec ID。
- 默认直接审查当前请求范围，包括适用的未提交修改；基线要可验证。独立审查只在用户要求并具备工具时进行。
- 每个发现说明触发条件、位置、影响和证据。修复反馈前核实事实；没有证据的疑问标为待验证，不冒充确定缺陷。
- 审查完成不自动 commit、push 或批准发布。

来源：mattpocock/skills `skills/engineering/code-review/SKILL.md`，修订 `c55ee46073ed923f86ce59a5eb3b6d895095d1b7`。授权见同目录 `mattpocock-LICENSE.txt`。这是本地适配版。
