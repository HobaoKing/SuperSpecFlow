# 分支与提交

遵循宿主项目约定。用户要求建分支时采用指定名称；没有约定时 Codex 使用 `codex/<topic>`，不要求 change-id / Spec ID。普通修改不自动建 worktree。

保护当前分支和无关未提交工作，不用 reset、clean 或强推绕过冲突。提交只精确暂存授权范围，核查最终 staged diff 和验证结果。

commit / PR 标题使用 `<英文类型>(<英文范围>): <中文摘要>`，正文按复杂度说明变更、验证和实际风险。提交、远端写入、合并与发布分别遵守已有授权。
