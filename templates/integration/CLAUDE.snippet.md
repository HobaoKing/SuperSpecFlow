# SuperSpecFlow 项目入口

仅在宿主不支持 include 时使用：将本段追加到已有项目指令，并把 `<pack>` 替换为 SuperSpecFlow 安装目录的绝对路径。

请读取项目 `.superspecflow/CLAUDE.routing.md`（若存在）；否则读取 `<pack>/routing/CLAUDE.routing.md`。按当前任务选择轻量方法，宿主业务规则优先，不要求完整阶段、变更编号或额外文件。保留宿主已有指令。
