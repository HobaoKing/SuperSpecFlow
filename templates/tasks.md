# Tasks: [change-id]

Fill guidance: each task should be independently reviewable and name the exact Spec ID, files, verification command, and acceptance signal.
Example: `T1: Add retry persistence test` links `SSF-EXAMPLE-001`, touches `tests/retry.test.ts`, and expects the test to fail before implementation.

- [ ] T1: [动词开头的任务]
  - Spec: [SPEC-ID]
  - Files: `path/to/file`
  - Test: [测试文件或验证命令]
  - Acceptance: [可直接验证的标准]
  - Estimate: N min
