Use the `ssf-git` skill.

Change id: $ARGUMENTS

Prepare a Chinese commit:
1. Check branch and working tree.
2. Confirm change-id and Spec IDs.
3. Review staged diff; if nothing staged, recommend selective staging.
4. Reject runtime or install artifacts such as `superpowers/`, `docs/superpowers/`, `.superspecflow/`, `.claude/`, `.codex/`, and `.DS_Store`.
5. Run or record verification.
6. Generate Chinese commit title and body.
7. Do not commit unrelated files.
